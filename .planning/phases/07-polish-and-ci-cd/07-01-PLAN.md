---
phase: 07-polish-and-ci-cd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/workspace/theme-toggle.tsx
  - src/components/workspace/sidebar.tsx
  - src/components/workspace/theme-toggle.test.tsx
autonomous: true
requirements: [THEME-01, THEME-02]
must_haves:
  truths:
    - "User can click a button in the sidebar to switch between light and dark mode"
    - "Theme defaults to system preference on first load (no manual config needed)"
    - "Theme preference persists across page refreshes via localStorage"
  artifacts:
    - path: "src/components/workspace/theme-toggle.tsx"
      provides: "ThemeToggle client component"
      exports: ["ThemeToggle"]
    - path: "src/components/workspace/sidebar.tsx"
      provides: "Sidebar with ThemeToggle mounted in bottom actions"
      contains: "ThemeToggle"
  key_links:
    - from: "src/components/workspace/theme-toggle.tsx"
      to: "next-themes"
      via: "useTheme() hook"
      pattern: "useTheme"
    - from: "src/components/workspace/sidebar.tsx"
      to: "src/components/workspace/theme-toggle.tsx"
      via: "import ThemeToggle"
      pattern: "import.*ThemeToggle"
---

<objective>
Add a dark mode toggle to the sidebar so users can switch between light and dark themes.

Purpose: THEME-01 (toggle) and THEME-02 (system default) — the ThemeProvider is already wired in layout.tsx with `defaultTheme="system"`, so THEME-02 is already implemented. This plan adds the visible toggle control and verifies both requirements.

Output: ThemeToggle component in sidebar bottom actions, unit test confirming render and toggle behavior.
</objective>

<execution_context>
@/home/daniel/Documentos/notely/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/Documentos/notely/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-polish-and-ci-cd/07-RESEARCH.md
@.planning/phases/07-polish-and-ci-cd/07-UI-SPEC.md

<interfaces>
<!-- ThemeProvider already in layout.tsx — do NOT modify layout.tsx -->
<!-- next-themes API: useTheme() returns { theme, setTheme, resolvedTheme, systemTheme } -->

From src/components/workspace/sidebar.tsx:
```typescript
// Bottom actions section (lines 76-102) — ThemeToggle goes between Trash link and UserMenu
<div className="px-2 py-2 space-y-1">
  <Button variant="ghost" className="w-full justify-start gap-2" onClick={() => createPage.mutate({})}>...</Button>
  <Link href={`/${orgSlug}/settings/members`}>...</Link>
  <Link href={`/${orgSlug}/trash`}>...</Link>
  <!-- INSERT ThemeToggle HERE -->
</div>
<UserMenu userName={userName} userImage={userImage} />
```

From src/components/ui/button.tsx:
```typescript
// shadcn Button — already installed, variant="ghost" size="icon" available
```

From src/app/layout.tsx:
```typescript
// ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange
// DO NOT MODIFY THIS FILE
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ThemeToggle component with unit test</name>
  <files>src/components/workspace/theme-toggle.tsx, src/components/workspace/theme-toggle.test.tsx</files>
  <read_first>
    - src/components/workspace/sidebar.tsx
    - src/app/layout.tsx
    - .planning/phases/07-polish-and-ci-cd/07-RESEARCH.md
    - .planning/phases/07-polish-and-ci-cd/07-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: ThemeToggle renders a button with aria-label containing "mode"
    - Test 2: Clicking the button calls setTheme — mock useTheme from next-themes
    - Test 3: When resolvedTheme is "dark", clicking calls setTheme("light")
    - Test 4: When resolvedTheme is "light", clicking calls setTheme("dark")
  </behavior>
  <action>
    Create `src/components/workspace/theme-toggle.tsx` as a `'use client'` component:

    1. Import `useEffect`, `useState` from react, `useTheme` from `next-themes`, `Button` from `@/components/ui/button`, `Moon` and `Sun` from `lucide-react`
    2. Use the `mounted` state pattern to prevent hydration mismatch:
       - `const [mounted, setMounted] = useState(false)`
       - `useEffect(() => setMounted(true), [])`
       - If not mounted, return a disabled placeholder `<Button variant="ghost" size="icon" className="h-11 w-11" disabled />`
    3. Use `resolvedTheme` (not `theme`) from `useTheme()` — resolves "system" to actual value
    4. Render `<Button variant="ghost" size="icon" className="h-11 w-11">` with:
       - `onClick={() => setTheme(resolvedTheme === 'dark' ? 'light' : 'dark')}`
       - `aria-label={resolvedTheme === 'dark' ? 'Switch to light mode' : 'Switch to dark mode'}`
       - Icon: `Sun` (h-4 w-4) when dark, `Moon` (h-4 w-4) when light

    Create `src/components/workspace/theme-toggle.test.tsx`:
    1. Mock `next-themes` with `vi.mock('next-themes', () => ({ useTheme: vi.fn() }))`
    2. Use `@testing-library/react` `render` and `screen`
    3. Write tests per behavior block above
    4. For mounted state: the component uses useEffect, so wrap render in `act` or use `waitFor`

    **THEME-02 verification (automated):** After creating the component, verify that `src/app/layout.tsx` contains both `defaultTheme="system"` and `enableSystem` in the ThemeProvider. This confirms THEME-02 is already wired. Add a verification step in the test file:
    - Test 5: Read layout.tsx and assert ThemeProvider has defaultTheme="system" — OR simply add to acceptance_criteria below as a grep check.
  </action>
  <verify>
    <automated>npx vitest run src/components/workspace/theme-toggle.test.tsx && grep -q 'defaultTheme="system"' src/app/layout.tsx && grep -q 'enableSystem' src/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/workspace/theme-toggle.tsx contains `'use client'`
    - src/components/workspace/theme-toggle.tsx contains `export function ThemeToggle`
    - src/components/workspace/theme-toggle.tsx contains `useTheme`
    - src/components/workspace/theme-toggle.tsx contains `resolvedTheme`
    - src/components/workspace/theme-toggle.tsx contains `aria-label`
    - src/components/workspace/theme-toggle.tsx contains `setMounted(true)`
    - src/components/workspace/theme-toggle.test.tsx contains `vi.mock('next-themes'`
    - npx vitest run src/components/workspace/theme-toggle.test.tsx exits 0
    - src/app/layout.tsx contains `defaultTheme="system"` (THEME-02 — system preference default)
    - src/app/layout.tsx contains `enableSystem` (THEME-02 — system preference detection enabled)
  </acceptance_criteria>
  <done>ThemeToggle component renders Sun/Moon icon, toggles theme on click, handles SSR hydration with mounted guard, all 4 unit tests pass, THEME-02 confirmed via layout.tsx containing defaultTheme="system" and enableSystem</done>
</task>

<task type="auto">
  <name>Task 2: Mount ThemeToggle in sidebar bottom actions</name>
  <files>src/components/workspace/sidebar.tsx</files>
  <read_first>
    - src/components/workspace/sidebar.tsx
    - src/components/workspace/theme-toggle.tsx
    - .planning/phases/07-polish-and-ci-cd/07-UI-SPEC.md
  </read_first>
  <action>
    Edit `src/components/workspace/sidebar.tsx`:

    1. Add import: `import { ThemeToggle } from './theme-toggle'`
    2. In the bottom actions `<div className="px-2 py-2 space-y-1">` block, add `<ThemeToggle />` as the last child AFTER the Trash link `</Link>` closing tag (line ~101) and BEFORE the closing `</div>` (line ~102). This places it between Trash and UserMenu per the UI-SPEC.

    The resulting JSX order in bottom actions:
    - New Page button
    - Settings link
    - Trash link
    - ThemeToggle (NEW)
    Then outside the div: UserMenu
  </action>
  <verify>
    <automated>npx vitest run --reporter=dot && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/workspace/sidebar.tsx contains `import { ThemeToggle } from './theme-toggle'`
    - src/components/workspace/sidebar.tsx contains `<ThemeToggle />`
    - npx tsc --noEmit exits 0
    - npx vitest run --reporter=dot exits 0
  </acceptance_criteria>
  <done>ThemeToggle visible in sidebar between Trash and UserMenu, TypeScript compiles cleanly, all existing tests still pass</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/workspace/theme-toggle.test.tsx` passes
- `npx tsc --noEmit` passes
- `npx vitest run --reporter=dot` all tests pass (no regressions)
- `grep -q 'defaultTheme="system"' src/app/layout.tsx` confirms THEME-02
</verification>

<success_criteria>
- ThemeToggle component exists at src/components/workspace/theme-toggle.tsx
- ThemeToggle renders in sidebar bottom actions section
- Clicking toggle switches between light and dark via next-themes
- System preference is honored by default (THEME-02 — verified by layout.tsx containing defaultTheme="system" and enableSystem)
- No hydration mismatch (mounted guard pattern)
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-and-ci-cd/07-01-SUMMARY.md`
</output>
