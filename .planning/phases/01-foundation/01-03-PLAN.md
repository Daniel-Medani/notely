---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/register/page.tsx
  - src/app/(auth)/layout.tsx
  - src/components/auth/sign-in-form.tsx
  - src/components/auth/sign-up-form.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "User can see a sign-in page at /login with Google OAuth button, 'or' divider, email/password form"
    - "User can see a sign-up page at /register with Google OAuth button, 'or' divider, name/email/password/confirmPassword form"
    - "Sign-in form submits via authClient.signIn.email() and redirects to /dashboard on success"
    - "Sign-up form submits via authClient.signUp.email() and redirects to /dashboard on success (D-15)"
    - "Form errors display inline below fields in red (D-13)"
    - "Submit button shows spinner and disables during submission (D-14)"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Sign-in page"
      contains: "Sign in to Notely"
    - path: "src/app/(auth)/register/page.tsx"
      provides: "Sign-up page"
      contains: "Create your account"
    - path: "src/components/auth/sign-in-form.tsx"
      provides: "Sign-in form component with RHF + Zod"
      contains: "signInSchema"
    - path: "src/components/auth/sign-up-form.tsx"
      provides: "Sign-up form component with RHF + Zod"
      contains: "signUpSchema"
  key_links:
    - from: "src/components/auth/sign-in-form.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.signIn.email"
      pattern: "authClient\\.signIn\\.email"
    - from: "src/components/auth/sign-up-form.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.signUp.email"
      pattern: "authClient\\.signUp\\.email"
    - from: "src/components/auth/sign-in-form.tsx"
      to: "src/lib/schemas/auth.ts"
      via: "zodResolver(signInSchema)"
      pattern: "zodResolver.*signInSchema"
    - from: "src/components/auth/sign-up-form.tsx"
      to: "src/lib/schemas/auth.ts"
      via: "zodResolver(signUpSchema)"
      pattern: "zodResolver.*signUpSchema"
---

<objective>
Build the sign-in and sign-up pages with full form validation, Google OAuth, loading states, and inline error display — exactly matching the UI-SPEC design contract.

Purpose: Users need to create accounts and sign in to access Notely. This is the entry point for every user.

Output: Two functional auth pages at /login and /register with all UI-SPEC interactions working.
</objective>

<execution_context>
@/home/daniel/Documentos/notely/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/Documentos/notely/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 02 that this plan uses -->

From src/lib/auth-client.ts:
```typescript
export const authClient = createAuthClient({
  baseURL: process.env.NEXT_PUBLIC_APP_URL,
  plugins: [organizationClient()],
})
// Methods: authClient.signIn.email(), authClient.signUp.email(), authClient.signIn.social(), authClient.signOut()
```

From src/lib/schemas/auth.ts:
```typescript
export const signUpSchema = z.object({ name, email, password, confirmPassword }).refine(...)
export const signInSchema = z.object({ email, password })
export type SignUpInput = z.infer<typeof signUpSchema>
export type SignInInput = z.infer<typeof signInSchema>
```

From shadcn/ui (installed in Plan 01):
- Button, Card, CardContent, CardHeader, CardTitle
- Input, Label
- Form, FormControl, FormField, FormItem, FormLabel, FormMessage
- Separator
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sign-up form component and register page</name>
  <files>
    src/components/auth/sign-up-form.tsx, src/app/(auth)/register/page.tsx, src/app/(auth)/layout.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md (Layout Contracts: Auth Pages, Interaction Contracts, Error Copywriting Contract, Copywriting Contract),
    src/lib/schemas/auth.ts (verify schema field names and error messages),
    src/lib/auth-client.ts (verify authClient export and methods),
    src/components/ui/button.tsx (verify Button component API),
    src/components/ui/card.tsx (verify Card component API),
    src/components/ui/form.tsx (verify Form component API and FormMessage for inline errors),
    src/components/ui/input.tsx (verify Input component API),
    src/components/ui/separator.tsx (verify Separator component API)
  </read_first>
  <action>
    1. Create `src/app/(auth)/layout.tsx` — shared auth layout (route group, no layout inheritance from workspace):
    ```tsx
    export default function AuthLayout({ children }: { children: React.ReactNode }) {
      return (
        <div className="flex min-h-screen items-center justify-center bg-background">
          {children}
        </div>
      )
    }
    ```
    Per D-02: centered card on a clean background.

    2. Create `src/components/auth/sign-up-form.tsx` — Client Component with React Hook Form + Zod:

    Must be `'use client'` at the top.

    Structure per UI-SPEC Layout Contract (Auth Pages):
    a. Brand text "Notely" — `text-[28px] font-semibold leading-[1.15] text-center mb-8` (Display typography)
    b. Title "Create your account" — `text-[20px] font-semibold leading-[1.2] text-center mb-6` (Heading typography)
    c. Google OAuth button — full-width, variant="outline", `mb-4`. Label: "Continue with Google". Use a Google SVG icon (lucide-react removed brand icons in v1, so use an inline SVG or Chrome icon as placeholder). On click: `authClient.signIn.social({ provider: 'google', callbackURL: '/dashboard' })`
    d. `<Separator>` with centered "or" text — implement with `<div className="relative my-4"><Separator /><span className="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 bg-card px-2 text-sm text-muted-foreground">or</span></div>`
    e. Form fields with `<Form>` wrapper and `useForm<SignUpInput>({ resolver: zodResolver(signUpSchema) })`:
       - Name field: `<FormField name="name">` with label "Name", placeholder "Enter your name"
       - Email field: `<FormField name="email">` with label "Email", placeholder "you@example.com"
       - Password field: `<FormField name="password">` with label "Password", type="password", placeholder "At least 8 characters"
       - Confirm password field: `<FormField name="confirmPassword">` with label "Confirm password", type="password"
       - Each field uses `<FormItem><FormLabel>...<FormControl><Input>...<FormMessage />` for D-13 inline errors (red text below field via FormMessage)
       - Fields stacked with `space-y-4` (16px gap per UI-SPEC)
    f. Submit button — full-width, variant="default" (primary), `mt-6`. Label: "Create account". Loading label: "Creating account..." with `<Loader2 className="mr-2 h-4 w-4 animate-spin" />`. Per D-14: button disabled + spinner during submission. All inputs disabled during submission.
    g. Cross-link — `mt-4 text-center text-sm`: "Already have an account? " + `<Link href="/login" className="underline">Sign in</Link>`

    On submit handler:
    - Call `authClient.signUp.email({ email, password, name, callbackURL: '/dashboard' })`
    - If error, check error message:
      - If contains "already exists" or "email taken" → `form.setError('email', { message: 'An account with this email already exists. Sign in instead.' })`
      - Otherwise → `form.setError('root', { message: 'Something went wrong. Please try again.' })`
    - If success → `router.push('/dashboard')` (D-15: redirect straight to workspace)

    Use `useState` for `isLoading` state to control button spinner and input disabled state.

    3. Create `src/app/(auth)/register/page.tsx`:
    ```tsx
    import { SignUpForm } from '@/components/auth/sign-up-form'

    export default function RegisterPage() {
      return <SignUpForm />
    }
    ```

    The page is a thin wrapper. The form component owns all logic.
  </action>
  <verify>
    <automated>cd /home/daniel/Documentos/notely && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(auth)/layout.tsx contains "min-h-screen"
    - src/app/(auth)/layout.tsx contains "items-center justify-center"
    - src/app/(auth)/layout.tsx contains "bg-background"
    - src/components/auth/sign-up-form.tsx starts with "'use client'"
    - src/components/auth/sign-up-form.tsx contains "signUpSchema" (imported from schemas)
    - src/components/auth/sign-up-form.tsx contains "zodResolver"
    - src/components/auth/sign-up-form.tsx contains "authClient.signUp.email"
    - src/components/auth/sign-up-form.tsx contains "Continue with Google"
    - src/components/auth/sign-up-form.tsx contains "Create your account" (heading, D-06)
    - src/components/auth/sign-up-form.tsx contains "Create account" (submit button label)
    - src/components/auth/sign-up-form.tsx contains "Creating account..." (loading label, D-14)
    - src/components/auth/sign-up-form.tsx contains "Loader2" (spinner icon, D-14)
    - src/components/auth/sign-up-form.tsx contains "animate-spin"
    - src/components/auth/sign-up-form.tsx contains "disabled" (inputs disabled during loading)
    - src/components/auth/sign-up-form.tsx contains "FormMessage" (inline errors, D-13)
    - src/components/auth/sign-up-form.tsx contains "Notely" (brand text, D-04)
    - src/components/auth/sign-up-form.tsx contains "Already have an account?"
    - src/components/auth/sign-up-form.tsx contains "/login" (cross-link, D-01)
    - src/components/auth/sign-up-form.tsx contains "Separator" (or divider, D-03)
    - src/components/auth/sign-up-form.tsx contains "router.push('/dashboard')" (D-15)
    - src/components/auth/sign-up-form.tsx contains "An account with this email already exists" (UI-SPEC error copy)
    - src/app/(auth)/register/page.tsx contains "SignUpForm"
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sign-up page renders at /register with all UI-SPEC elements: brand text, heading, Google OAuth button, or divider, 4-field form with inline validation, loading spinner, and cross-link to /login.</done>
</task>

<task type="auto">
  <name>Task 2: Create sign-in form component and login page</name>
  <files>
    src/components/auth/sign-in-form.tsx, src/app/(auth)/login/page.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md (Layout Contracts: Auth Pages, Interaction Contracts, Error Copywriting Contract, Copywriting Contract),
    src/lib/schemas/auth.ts (verify signInSchema fields),
    src/lib/auth-client.ts (verify authClient.signIn.email method),
    src/components/auth/sign-up-form.tsx (reuse same patterns for consistency — Card wrapper, OAuth button, Separator, Form structure)
  </read_first>
  <action>
    1. Create `src/components/auth/sign-in-form.tsx` — Client Component, same structure as sign-up but with 2 fields:

    Must be `'use client'` at the top.

    Structure per UI-SPEC Layout Contract:
    a. Brand text "Notely" — same Display typography as sign-up
    b. Title "Sign in to Notely" — Heading typography, centered, `mb-6`
    c. Google OAuth button — same as sign-up: full-width, outline variant, "Continue with Google", `mb-4`. On click: `authClient.signIn.social({ provider: 'google', callbackURL: '/dashboard' })`
    d. Separator with "or" — same pattern as sign-up
    e. Form with `useForm<SignInInput>({ resolver: zodResolver(signInSchema) })`:
       - Email field: label "Email", placeholder "you@example.com"
       - Password field: label "Password", type="password"
       - Fields stacked with `space-y-4`
       - Each field uses FormItem > FormLabel > FormControl > Input > FormMessage (D-13)
    f. Submit button — "Sign in", loading: "Signing in..." with Loader2 spinner (D-14)
    g. Cross-link — "Don't have an account? " + `<Link href="/register">Create one</Link>`

    On submit handler:
    - Call `authClient.signIn.email({ email, password, callbackURL: '/dashboard' })`
    - If error → `form.setError('root', { message: 'Incorrect email or password.' })` (UI-SPEC error copy — combined message per SEC-06)
    - If success → `router.push('/dashboard')`

    For root-level errors (form.formState.errors.root), display above the submit button:
    ```tsx
    {form.formState.errors.root && (
      <p className="text-sm text-destructive text-center">
        {form.formState.errors.root.message}
      </p>
    )}
    ```

    The Card wrapper pattern: wrap entire form content in `<Card className="w-full max-w-sm"><CardContent className="p-8">...</CardContent></Card>` — per D-02.

    2. Create `src/app/(auth)/login/page.tsx`:
    ```tsx
    import { SignInForm } from '@/components/auth/sign-in-form'

    export default function LoginPage() {
      return <SignInForm />
    }
    ```
  </action>
  <verify>
    <automated>cd /home/daniel/Documentos/notely && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/auth/sign-in-form.tsx starts with "'use client'"
    - src/components/auth/sign-in-form.tsx contains "signInSchema"
    - src/components/auth/sign-in-form.tsx contains "zodResolver"
    - src/components/auth/sign-in-form.tsx contains "authClient.signIn.email"
    - src/components/auth/sign-in-form.tsx contains "Continue with Google"
    - src/components/auth/sign-in-form.tsx contains "Sign in to Notely"
    - src/components/auth/sign-in-form.tsx contains "Sign in" (submit button — not "Sign In" capitalized)
    - src/components/auth/sign-in-form.tsx contains "Signing in..." (loading label)
    - src/components/auth/sign-in-form.tsx contains "Loader2"
    - src/components/auth/sign-in-form.tsx contains "animate-spin"
    - src/components/auth/sign-in-form.tsx contains "disabled"
    - src/components/auth/sign-in-form.tsx contains "FormMessage"
    - src/components/auth/sign-in-form.tsx contains "Notely"
    - src/components/auth/sign-in-form.tsx contains "Don't have an account?"
    - src/components/auth/sign-in-form.tsx contains "/register"
    - src/components/auth/sign-in-form.tsx contains "Separator"
    - src/components/auth/sign-in-form.tsx contains "Incorrect email or password."
    - src/components/auth/sign-in-form.tsx contains "max-w-sm" (D-02 card width)
    - src/components/auth/sign-in-form.tsx contains "router.push('/dashboard')"
    - src/app/(auth)/login/page.tsx contains "SignInForm"
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sign-in page renders at /login with all UI-SPEC elements: brand text, heading, Google OAuth button, or divider, 2-field form with inline validation, loading spinner, and cross-link to /register.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with all auth UI files
- Both auth pages use the (auth) route group layout
- Forms import shared Zod schemas from src/lib/schemas/auth.ts
- Forms use authClient (not Server Actions) for auth calls
- All error messages match UI-SPEC Error Copywriting Contract exactly
- Loading states match UI-SPEC Interaction Contract (D-14)
</verification>

<success_criteria>
- /login page renders sign-in form with Google OAuth + email/password
- /register page renders sign-up form with Google OAuth + 4-field email/password form
- Inline validation errors display below fields in red (D-13)
- Submit buttons show spinner + "Signing in..."/"Creating account..." during load (D-14)
- Cross-links navigate between /login and /register (D-01)
- Card centered on clean background (D-02)
- Google OAuth button above form with "or" divider (D-03)
- Brand text "Notely" displayed as app name only (D-04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
